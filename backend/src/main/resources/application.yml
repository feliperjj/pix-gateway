spring:
  application:
    name: pixgateway-backend
  
  datasource:
    url: jdbc:postgresql://localhost:5433/pixgateway_db
    username: felipe
    password: pixpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true